class Version {
    constructor(id, type, version, createdAt) {
        this.id = id;
        this.type = type;
        this.version = version;
        this.createdAt = createdAt;
    }
}

module.exports = Version;
